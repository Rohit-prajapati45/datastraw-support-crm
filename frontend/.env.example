# Render build-time variable. Leave unset locally to use Vite's /api proxy.
# On Render, set this to the Railway backend URL without a trailing slash.
VITE_API_URL=https://your-backend.up.railway.app
